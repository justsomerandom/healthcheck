# Protocol

This document will describe the server/agent protocol, compatibility expectations, authentication model, and message formats once protocol work begins.

The agent/server wire contract will be defined after local and remote check execution semantics are established, so transport models are derived from real behavior rather than speculative requirements. Implementation-only server executor types should not be copied into protocol DTOs.
